<UserControl x:Class="OpenSAE.Views.SymbolArtBrowser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fb="clr-namespace:OpenSAE.Models.FileBrowser" xmlns:views="clr-namespace:OpenSAE.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=fb:FileBrowserModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <ListView ItemsSource="{Binding Files}">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Preview" Width="200">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding SymbolImage}" Width="{Binding SymbolArt.Width}" Height="{Binding SymbolArt.Height}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="Filename" DisplayMemberBinding="{Binding FileName}" Width="250" />
                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="250" />
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
